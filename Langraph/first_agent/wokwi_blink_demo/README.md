# Wokwi Arduino Uno Blink Demo

`wokwi_blink_demo.ino` blinks an external LED wired to Arduino Uno pin 13.

Run `./build.sh` to create the firmware files in `build/`, then open this folder in VS Code and run `Wokwi: Start Simulator`.
